{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "VI Notes (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run dev server"
    }
  ]
}
